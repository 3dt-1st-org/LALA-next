import 'package:flutter/material.dart';

/// 장소 맥락 팩트 데이터(C3 추출 — main.dart 의 _ContextFact).
class ContextFact {
  const ContextFact({required this.icon, required this.label});

  final IconData icon;
  final String label;
}
